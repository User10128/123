{
  "name": "Checkers",
  "version": "1.0.0",
  "description": "Checkers game",
  "scripts": {
    "build": "mkdir -p dist && cp index.html admin-panel.html firestore.rules dist/ && (cp background.png dist/ || true)",
    "start": "echo 'Static site served from dist/'"
  },
  "dependencies": {},
  "devDependencies": {}
}
