// This file can be mostly empty, but the 'fetch' event is required for installation
self.addEventListener('fetch', (event) => {
  // Satisfies the browser's requirement for a functional service worker
});
